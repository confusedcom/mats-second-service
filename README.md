# mats-second-service

Dummy service

## Overview

- **Language**: Go
- **Team**: ★ Developer Experience And Enablement
- **Deployment**: Rolling
- **Ingress**: false


## Getting Started

1. Build the application:
   ```bash
   docker build -t mats-second-service .
   ```

2. Run locally:
   ```bash
   docker run -p 8080:8080 mats-second-service
   ```

3. Deploy to Kubernetes:
   ```bash
   kubectl apply -f k8s/
   ```

## Project Structure

- `Dockerfile` - Container image definition
- `k8s/` - Kubernetes manifests
  - `deployment.yaml` - Deployment configuration
  - `service.yaml` - Service configuration
  - `ingress.yaml` - Ingress configuration (if enabled)
  - `rollout.yaml` - Argo Rollout strategy
- `argocd/` - ArgoCD application configuration
- `catalog-info.yaml` - Backstage service catalog
- `.snyk` - Snyk security policy

## Development

Generated by [conf CLI](https://github.com/confusedcom/conf)
